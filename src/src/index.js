import React from "react";
import ReactDOM from "react-dom/client";
import OperationalEvaluationQuiz from "./OperationalEvaluationQuiz";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <OperationalEvaluationQuiz />
  </React.StrictMode>
);
